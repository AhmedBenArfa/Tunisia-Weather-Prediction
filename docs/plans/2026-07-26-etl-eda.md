# Phase ETL / EDA — Plan d'implémentation

> **Pour un intervenant sans contexte :** exécuter tâche par tâche, dans
> l'ordre. Les étapes utilisent des cases à cocher (`- [ ]`) pour le suivi.

**Objectif :** valider la qualité du jeu horaire, corriger les anomalies
connues, mesurer le décalage entre la source d'entraînement et la source de
production sur les 24 gouvernorats, et livrer un notebook d'analyse
exploratoire reproductible.

**Architecture :** des modules courts à responsabilité unique dans `01_etl/`,
orchestrés par `run_etl.py`. Les fonctions pures (contrôles, nettoyage, calcul
de métriques) sont testées avec des données synthétiques ; seul
`skew_analysis.py` touche le réseau, et sa partie calcul en est isolée pour
rester testable. Le notebook est **généré** par un script versionné, jamais
édité à la main.

**Stack :** pandas 3.0, pyarrow, requests, matplotlib, nbformat, pytest.

## Contraintes globales

- **Git : préparer et `git add` uniquement.** Ahmed Ben Arfa commite et pousse
  lui-même. Ne jamais commiter, ne jamais s'attribuer une contribution.
- **Aucun accent dans le code source** (commentaires compris) — problèmes
  d'encodage sous Windows. Les fichiers `.md` sont accentués normalement.
- **Chemins relatifs** construits avec `pathlib.Path`, jamais d'absolu en dur.
- **Idempotence** : tout script relançable sans effet de bord.
- **Aucune valeur mesurée dans la documentation** tant qu'elle n'est pas
  produite par du code versionné.
- Interpréteur : `.venv\Scripts\python.exe`, toujours lancé depuis la racine du
  dépôt.
- Les 24 gouvernorats forment **24 séries parallèles** : toute opération
  temporelle passe par `groupby("gouvernorat")`.

## Structure des fichiers

| Fichier | Responsabilité |
|---|---|
| `01_etl/config.py` | Chemins, constantes, plages physiques attendues |
| `01_etl/checks.py` | Contrôles de qualité, sans effet de bord |
| `01_etl/clean.py` | Corrections des anomalies connues |
| `01_etl/skew_analysis.py` | Comparaison archive / forecast sur 24 gouvernorats |
| `01_etl/run_etl.py` | Orchestrateur : charge → contrôle → nettoie → écrit |
| `01_etl/notebooks/_build_eda.py` | Générateur du notebook |
| `tests/test_checks.py` | Tests des contrôles |
| `tests/test_clean.py` | Tests du nettoyage |
| `tests/test_skew.py` | Tests du calcul de métriques de décalage |

**Note d'écart avec la conception.** Le document de conception plaçait le
nettoyage en phase 02. Il est ramené en 01, comme dans `ProjetIntegre` où
`transform.py` nettoie avant chargement. La phase 02 partira du Parquet
nettoyé. Mettre à jour `docs/conception/2026-07-26-conception-projet.md` §2 en
fin de phase.

---

### Task 1 : Socle de configuration et contrôles de qualité

**Files:**
- Create: `01_etl/config.py`
- Create: `01_etl/checks.py`
- Test: `tests/test_checks.py`
- Modify: `requirements.txt` (ajout de `pytest`)

**Interfaces:**
- Produces:
  - `config.RACINE: Path`, `config.DATA: Path`, `config.PARQUET_BRUT: Path`,
    `config.PARQUET_PROPRE: Path`
  - `config.PLAGES: dict[str, tuple[float, float]]`
  - `config.NB_GOUVERNORATS: int`, `config.NB_HEURES_ATTENDUES: int`
  - `checks.verifier_completude(df) -> list[str]`
  - `checks.verifier_doublons(df) -> list[str]`
  - `checks.verifier_continuite_horaire(df) -> list[str]`
  - `checks.verifier_plages_physiques(df) -> list[str]`
  - `checks.verifier_decalage_groupe(df, colonne, horizon) -> list[str]`
  - `checks.executer_controles(df) -> list[str]`

  Chaque fonction retourne la **liste des anomalies détectées**, vide si tout
  va bien. Aucune n'affiche ni ne lève.

- [ ] **Step 1 : Ajouter pytest aux dépendances**

Dans `requirements.txt`, sous la section outils, ajouter :

```
# Tests
pytest>=8.0
```

Puis installer :

```bash
uv pip install --python .venv/Scripts/python.exe pytest
```

- [ ] **Step 2 : Écrire `01_etl/config.py`**

```python
"""Chemins et constantes partages par les modules ETL."""

from pathlib import Path

RACINE = Path(__file__).resolve().parent.parent
DATA = RACINE / "data"

PARQUET_BRUT = DATA / "tunisia_weather_hourly.parquet"
PARQUET_PROPRE = DATA / "tunisia_weather_clean.parquet"
SKEW_CSV = DATA / "skew_era5_forecast.csv"

NB_GOUVERNORATS = 24
NB_HEURES_ATTENDUES = 66_072          # 2019-01-01 -> 2026-07-15, pas horaire

# Plages physiquement admissibles, volontairement larges : on cherche
# l'aberration franche, pas la valeur rare.
PLAGES = {
    "temperature_2m": (-20.0, 60.0),
    "apparent_temperature": (-30.0, 65.0),
    "dew_point_2m": (-40.0, 40.0),
    "relative_humidity_2m": (0.0, 100.0),
    "precipitation": (0.0, 200.0),
    "rain": (0.0, 200.0),
    "snowfall": (0.0, 100.0),
    "pressure_msl": (900.0, 1100.0),
    "surface_pressure": (850.0, 1100.0),
    "cloud_cover": (0.0, 100.0),
    "cloud_cover_low": (0.0, 100.0),
    "cloud_cover_mid": (0.0, 100.0),
    "cloud_cover_high": (0.0, 100.0),
    "wind_speed_10m": (0.0, 250.0),
    "wind_speed_100m": (0.0, 300.0),
    "wind_direction_10m": (0.0, 360.0),
    "wind_direction_100m": (0.0, 360.0),
    "wind_gusts_10m": (0.0, 350.0),
    "soil_moisture_0_to_7cm": (0.0, 1.0),
    "soil_moisture_7_to_28cm": (0.0, 1.0),
    "soil_moisture_28_to_100cm": (0.0, 1.0),
    "shortwave_radiation": (0.0, 1400.0),
    "direct_radiation": (0.0, 1400.0),
    "diffuse_radiation": (0.0, 1400.0),
}
```

- [ ] **Step 3 : Écrire le test des contrôles (il doit échouer)**

Créer `tests/test_checks.py` :

```python
"""Tests des controles de qualite, sur donnees synthetiques."""

import sys
from pathlib import Path

import pandas as pd
import pytest

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "01_etl"))

import checks  # noqa: E402


def jeu_valide(nb_heures=48, gouvernorats=("Tunis", "Sfax")):
    """Deux series horaires continues, sans anomalie."""
    lignes = []
    for nom in gouvernorats:
        temps = pd.date_range("2020-01-01", periods=nb_heures, freq="h")
        lignes.append(pd.DataFrame({
            "gouvernorat": nom,
            "time": temps,
            "temperature_2m": 20.0,
            "relative_humidity_2m": 50.0,
            "soil_moisture_0_to_7cm": 0.2,
        }))
    return pd.concat(lignes, ignore_index=True)


def test_jeu_valide_ne_remonte_aucune_anomalie():
    assert checks.verifier_completude(jeu_valide()) == []
    assert checks.verifier_doublons(jeu_valide()) == []
    assert checks.verifier_continuite_horaire(jeu_valide()) == []
    assert checks.verifier_plages_physiques(jeu_valide()) == []


def test_valeur_manquante_detectee():
    df = jeu_valide()
    df.loc[5, "temperature_2m"] = None
    anomalies = checks.verifier_completude(df)
    assert len(anomalies) == 1
    assert "temperature_2m" in anomalies[0]


def test_doublon_detecte():
    df = jeu_valide()
    df = pd.concat([df, df.iloc[[0]]], ignore_index=True)
    assert len(checks.verifier_doublons(df)) == 1


def test_trou_horaire_detecte():
    df = jeu_valide()
    df = df.drop(index=10).reset_index(drop=True)
    anomalies = checks.verifier_continuite_horaire(df)
    assert len(anomalies) == 1
    assert "Tunis" in anomalies[0]


def test_valeur_hors_plage_detectee():
    df = jeu_valide()
    df.loc[3, "soil_moisture_0_to_7cm"] = -0.01
    anomalies = checks.verifier_plages_physiques(df)
    assert len(anomalies) == 1
    assert "soil_moisture_0_to_7cm" in anomalies[0]


def test_decalage_groupe_correct_accepte():
    """Un decalage calcule par gouvernorat laisse H valeurs manquantes par serie."""
    df = jeu_valide()
    df["lag"] = df.groupby("gouvernorat", observed=True)["temperature_2m"].shift(24)
    assert checks.verifier_decalage_groupe(df, "lag", 24) == []


def test_decalage_global_detecte_comme_faux():
    """Un decalage global contamine la frontiere entre gouvernorats."""
    df = jeu_valide().sort_values(["gouvernorat", "time"]).reset_index(drop=True)
    df["lag"] = df["temperature_2m"].shift(24)
    anomalies = checks.verifier_decalage_groupe(df, "lag", 24)
    assert len(anomalies) == 1
    assert "Tunis" in anomalies[0] or "Sfax" in anomalies[0]
```

- [ ] **Step 4 : Lancer les tests et vérifier qu'ils échouent**

```bash
.venv/Scripts/python.exe -m pytest tests/test_checks.py -v
```

Attendu : `ModuleNotFoundError: No module named 'checks'`.

- [ ] **Step 5 : Écrire `01_etl/checks.py`**

```python
"""
Controles de qualite du jeu horaire.

Chaque fonction retourne la liste des anomalies detectees, vide si le jeu est
conforme. Aucune n'affiche ni ne leve : l'orchestrateur decide quoi en faire.
"""

from __future__ import annotations

import pandas as pd

import config


def verifier_completude(df: pd.DataFrame) -> list[str]:
    """Aucune valeur manquante attendue."""
    anomalies = []
    manquants = df.isna().sum()
    for colonne, nb in manquants[manquants > 0].items():
        anomalies.append(f"{colonne} : {nb:,} valeurs manquantes")
    return anomalies


def verifier_doublons(df: pd.DataFrame) -> list[str]:
    """Le couple (gouvernorat, heure) doit etre unique."""
    nb = df.duplicated(["gouvernorat", "time"]).sum()
    if nb:
        return [f"{nb:,} doublons sur (gouvernorat, time)"]
    return []


def verifier_continuite_horaire(df: pd.DataFrame) -> list[str]:
    """Chaque serie doit avancer d'exactement une heure a chaque pas."""
    anomalies = []
    for nom, groupe in df.sort_values("time").groupby("gouvernorat", observed=True):
        ecarts = groupe["time"].diff().dropna()
        irreguliers = ecarts[ecarts != pd.Timedelta(hours=1)]
        if len(irreguliers):
            anomalies.append(
                f"{nom} : {len(irreguliers)} intervalle(s) different(s) d'une heure"
            )
    return anomalies


def verifier_plages_physiques(df: pd.DataFrame) -> list[str]:
    """Les valeurs doivent rester dans des bornes physiquement admissibles."""
    anomalies = []
    for colonne, (mini, maxi) in config.PLAGES.items():
        if colonne not in df.columns:
            continue
        hors = ((df[colonne] < mini) | (df[colonne] > maxi)).sum()
        if hors:
            anomalies.append(
                f"{colonne} : {hors:,} valeurs hors de [{mini}, {maxi}] "
                f"(observe {df[colonne].min():.3f} a {df[colonne].max():.3f})"
            )
    return anomalies


def verifier_decalage_groupe(df: pd.DataFrame, colonne: str,
                             horizon: int) -> list[str]:
    """Verifie qu'un decalage a bien ete calcule par gouvernorat.

    Un decalage groupe laisse exactement `horizon` valeurs manquantes au debut
    de chaque serie. Un decalage global n'en laisse qu'au tout premier
    gouvernorat : les suivants recuperent silencieusement les dernieres heures
    du precedent. C'est une erreur sans exception, invisible sans ce controle.
    """
    anomalies = []
    for nom, groupe in df.sort_values("time").groupby("gouvernorat", observed=True):
        nb_manquants = groupe[colonne].head(horizon).isna().sum()
        if nb_manquants != horizon:
            anomalies.append(
                f"{nom} : {nb_manquants}/{horizon} valeurs manquantes en tete de "
                f"'{colonne}' — le decalage n'a pas ete groupe par gouvernorat"
            )
    return anomalies


def executer_controles(df: pd.DataFrame) -> list[str]:
    """Enchaine tous les controles structurels."""
    anomalies = []
    anomalies += verifier_completude(df)
    anomalies += verifier_doublons(df)
    anomalies += verifier_continuite_horaire(df)
    anomalies += verifier_plages_physiques(df)

    nb_gouv = df["gouvernorat"].nunique()
    if nb_gouv != config.NB_GOUVERNORATS:
        anomalies.append(
            f"{nb_gouv} gouvernorats au lieu de {config.NB_GOUVERNORATS}"
        )
    return anomalies
```

- [ ] **Step 6 : Lancer les tests et vérifier qu'ils passent**

```bash
.venv/Scripts/python.exe -m pytest tests/test_checks.py -v
```

Attendu : 7 tests PASS.

- [ ] **Step 7 : Lancer les contrôles sur le vrai jeu**

```bash
.venv/Scripts/python.exe -c "import sys; sys.path.insert(0,'01_etl'); import pandas as pd, config, checks; df=pd.read_parquet(config.PARQUET_BRUT); [print(a) for a in checks.executer_controles(df)] or print('aucune anomalie')"
```

Attendu : une seule famille d'anomalies, les trois colonnes
`soil_moisture_*` hors plage (valeurs négatives). C'est le défaut connu que la
tâche 2 corrige. **Noter les nombres exacts affichés** — ils alimenteront le
notebook.

- [ ] **Step 8 : Stager**

```bash
git add 01_etl/config.py 01_etl/checks.py tests/test_checks.py requirements.txt
```

---

### Task 2 : Nettoyage des anomalies connues

**Files:**
- Create: `01_etl/clean.py`
- Test: `tests/test_clean.py`

**Interfaces:**
- Consumes: `config.PLAGES`
- Produces:
  - `clean.corriger_humidite_sol(df) -> tuple[pd.DataFrame, list[str]]`
  - `clean.nettoyer(df) -> tuple[pd.DataFrame, list[str]]`

  Les deux retournent le tableau corrigé et le journal des corrections
  appliquées. Aucune ne modifie le tableau reçu.

- [ ] **Step 1 : Écrire le test (il doit échouer)**

Créer `tests/test_clean.py` :

```python
"""Tests du nettoyage, sur donnees synthetiques."""

import sys
from pathlib import Path

import pandas as pd

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "01_etl"))

import clean  # noqa: E402


def jeu_avec_humidite_negative():
    return pd.DataFrame({
        "gouvernorat": ["Tozeur"] * 4,
        "time": pd.date_range("2020-01-01", periods=4, freq="h"),
        "soil_moisture_0_to_7cm": [0.20, -0.013, 0.18, -0.001],
        "soil_moisture_7_to_28cm": [0.22, 0.21, -0.012, 0.20],
        "soil_moisture_28_to_100cm": [0.19, 0.19, 0.19, 0.19],
    })


def test_humidite_negative_ramenee_a_zero():
    df, journal = clean.corriger_humidite_sol(jeu_avec_humidite_negative())
    for colonne in [c for c in df.columns if c.startswith("soil_moisture")]:
        assert (df[colonne] >= 0).all()
    assert len(journal) == 2          # deux couches concernees


def test_valeurs_positives_inchangees():
    origine = jeu_avec_humidite_negative()
    df, _ = clean.corriger_humidite_sol(origine)
    assert df.loc[0, "soil_moisture_0_to_7cm"] == 0.20
    assert df.loc[2, "soil_moisture_0_to_7cm"] == 0.18


def test_tableau_origine_non_modifie():
    origine = jeu_avec_humidite_negative()
    clean.corriger_humidite_sol(origine)
    assert origine.loc[1, "soil_moisture_0_to_7cm"] == -0.013


def test_journal_mentionne_le_nombre_corrige():
    _, journal = clean.corriger_humidite_sol(jeu_avec_humidite_negative())
    assert any("soil_moisture_0_to_7cm" in ligne and "2" in ligne
               for ligne in journal)


def test_jeu_sans_anomalie_produit_journal_vide():
    df = jeu_avec_humidite_negative()
    df[[c for c in df.columns if c.startswith("soil_moisture")]] = 0.2
    _, journal = clean.corriger_humidite_sol(df)
    assert journal == []
```

- [ ] **Step 2 : Lancer le test et vérifier qu'il échoue**

```bash
.venv/Scripts/python.exe -m pytest tests/test_clean.py -v
```

Attendu : `ModuleNotFoundError: No module named 'clean'`.

- [ ] **Step 3 : Écrire `01_etl/clean.py`**

```python
"""
Corrections des anomalies connues du jeu horaire.

Chaque fonction retourne une copie corrigee et le journal des corrections,
pour que l'orchestrateur puisse tracer ce qui a ete modifie.
"""

from __future__ import annotations

import pandas as pd

COUCHES_HUMIDITE = [
    "soil_moisture_0_to_7cm",
    "soil_moisture_7_to_28cm",
    "soil_moisture_28_to_100cm",
]


def corriger_humidite_sol(df: pd.DataFrame) -> tuple[pd.DataFrame, list[str]]:
    """Ramene a zero les humidites de sol negatives.

    Une teneur en eau volumique ne peut pas etre negative. Les valeurs sous
    zero sont un artefact numerique de la reanalyse sur sols tres secs, ou la
    grandeur oscille autour de zero. Elles se concentrent sur les gouvernorats
    arides du sud.
    """
    corrige = df.copy()
    journal = []

    for colonne in COUCHES_HUMIDITE:
        if colonne not in corrige.columns:
            continue
        negatives = corrige[colonne] < 0
        nb = int(negatives.sum())
        if nb:
            mini = corrige.loc[negatives, colonne].min()
            corrige.loc[negatives, colonne] = 0.0
            journal.append(
                f"{colonne} : {nb:,} valeurs negatives ramenees a 0 "
                f"(minimum observe {mini:.4f})"
            )

    return corrige, journal


def nettoyer(df: pd.DataFrame) -> tuple[pd.DataFrame, list[str]]:
    """Enchaine toutes les corrections."""
    corrige, journal = corriger_humidite_sol(df)
    return corrige, journal
```

- [ ] **Step 4 : Lancer les tests et vérifier qu'ils passent**

```bash
.venv/Scripts/python.exe -m pytest tests/test_clean.py -v
```

Attendu : 5 tests PASS.

- [ ] **Step 5 : Stager**

```bash
git add 01_etl/clean.py tests/test_clean.py
```

---

### Task 3 : Orchestrateur ETL

**Files:**
- Create: `01_etl/run_etl.py`

**Interfaces:**
- Consumes: `config`, `checks.executer_controles`, `clean.nettoyer`
- Produces: `data/tunisia_weather_clean.parquet`

- [ ] **Step 1 : Écrire `01_etl/run_etl.py`**

```python
"""
Orchestrateur ETL : charge le Parquet brut, controle, nettoie, controle a
nouveau, ecrit le Parquet propre.

Idempotent : relançable sans effet de bord.

Usage :
    python 01_etl/run_etl.py
"""

from __future__ import annotations

import sys
from pathlib import Path

import pandas as pd

sys.path.insert(0, str(Path(__file__).resolve().parent))

import checks
import clean
import config


def afficher(titre: str, lignes: list[str]) -> None:
    print(f"\n--- {titre} ---")
    if not lignes:
        print("  aucune anomalie")
        return
    for ligne in lignes:
        print(f"  ! {ligne}")


def main() -> int:
    if not config.PARQUET_BRUT.exists():
        print(f"Introuvable : {config.PARQUET_BRUT}")
        print("Lancer d'abord 01_etl/build_parquet.py")
        return 1

    print(f"Lecture de {config.PARQUET_BRUT.name}")
    df = pd.read_parquet(config.PARQUET_BRUT)
    print(f"  {len(df):,} lignes x {df.shape[1]} colonnes")

    afficher("Controles avant nettoyage", checks.executer_controles(df))

    df, journal = clean.nettoyer(df)
    afficher("Corrections appliquees", journal)

    restantes = checks.executer_controles(df)
    afficher("Controles apres nettoyage", restantes)

    if restantes:
        print("\nDes anomalies subsistent : le Parquet propre n'est pas ecrit.")
        return 1

    df.to_parquet(config.PARQUET_PROPRE, compression="zstd", index=False)
    taille = config.PARQUET_PROPRE.stat().st_size / 1e6
    print(f"\n--- Termine ---")
    print(f"Ecrit : {config.PARQUET_PROPRE.name}  ({taille:.1f} Mo)")
    print(f"  lignes   : {len(df):,}")
    print(f"  periode  : {df['time'].min()} -> {df['time'].max()}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2 : Exécuter sur le vrai jeu**

```bash
.venv/Scripts/python.exe -u 01_etl/run_etl.py
```

Attendu : anomalies `soil_moisture_*` avant nettoyage, corrections journalisées,
aucune anomalie après, écriture de `tunisia_weather_clean.parquet`. Code de
sortie 0.

- [ ] **Step 3 : Vérifier que le fichier produit est cohérent**

```bash
.venv/Scripts/python.exe -c "import pandas as pd; d=pd.read_parquet('data/tunisia_weather_clean.parquet'); print(len(d), d.shape[1]); print(d[[c for c in d.columns if c.startswith('soil_moisture')]].min())"
```

Attendu : 1585728 lignes, 36 colonnes, minima d'humidité de sol tous ≥ 0.

- [ ] **Step 4 : Stager**

```bash
git add 01_etl/run_etl.py data/tunisia_weather_clean.parquet
```

---

### Task 4 : Décalage entre source d'entraînement et source de production

**Files:**
- Create: `01_etl/skew_analysis.py`
- Test: `tests/test_skew.py`

**Interfaces:**
- Consumes: `config.PARQUET_PROPRE`, `config.SKEW_CSV`
- Produces:
  - `skew_analysis.calculer_metriques(archive, forecast, variables) -> pd.DataFrame`
    → colonnes `variable`, `n`, `biais`, `mae`, `sigma`, `mae_sur_sigma`
  - `skew_analysis.recuperer_forecast(lat, lon, variables, jours) -> pd.DataFrame`
  - `data/skew_era5_forecast.csv`

**Note quota.** 24 requêtes × 31 variables × 92 jours ≈ 490 appels pondérés sur
un plafond de 10 000/jour. Sans risque, mais garder la pause entre requêtes.

- [ ] **Step 1 : Écrire le test du calcul (il doit échouer)**

Créer `tests/test_skew.py` :

```python
"""Tests du calcul de metriques de decalage, sans reseau."""

import sys
from pathlib import Path

import pandas as pd

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "01_etl"))

import skew_analysis  # noqa: E402


def paire_synthetique():
    """Le forecast est decale de +2 par rapport a l'archive."""
    temps = pd.date_range("2026-05-01", periods=100, freq="h")
    archive = pd.DataFrame({
        "gouvernorat": "Tunis",
        "time": temps,
        "temperature_2m": [20.0 + i % 10 for i in range(100)],
    })
    forecast = archive.copy()
    forecast["temperature_2m"] = forecast["temperature_2m"] + 2.0
    return archive, forecast


def test_biais_constant_detecte():
    archive, forecast = paire_synthetique()
    res = skew_analysis.calculer_metriques(archive, forecast, ["temperature_2m"])
    ligne = res.iloc[0]
    assert ligne["variable"] == "temperature_2m"
    assert abs(ligne["biais"] - 2.0) < 1e-9
    assert abs(ligne["mae"] - 2.0) < 1e-9
    assert ligne["n"] == 100


def test_sources_identiques_donnent_zero():
    archive, _ = paire_synthetique()
    res = skew_analysis.calculer_metriques(archive, archive, ["temperature_2m"])
    assert abs(res.iloc[0]["biais"]) < 1e-9
    assert abs(res.iloc[0]["mae"]) < 1e-9
    assert abs(res.iloc[0]["mae_sur_sigma"]) < 1e-9


def test_seules_les_heures_communes_comptent():
    archive, forecast = paire_synthetique()
    forecast = forecast.iloc[:40]
    res = skew_analysis.calculer_metriques(archive, forecast, ["temperature_2m"])
    assert res.iloc[0]["n"] == 40


def test_resultat_trie_par_mae_sur_sigma():
    temps = pd.date_range("2026-05-01", periods=50, freq="h")
    archive = pd.DataFrame({
        "gouvernorat": "Tunis", "time": temps,
        "a": [float(i % 7) for i in range(50)],
        "b": [float(i % 7) for i in range(50)],
    })
    forecast = archive.copy()
    forecast["a"] = forecast["a"] + 0.1
    forecast["b"] = forecast["b"] + 3.0
    res = skew_analysis.calculer_metriques(archive, forecast, ["a", "b"])
    assert list(res["variable"]) == ["a", "b"]
```

- [ ] **Step 2 : Lancer le test et vérifier qu'il échoue**

```bash
.venv/Scripts/python.exe -m pytest tests/test_skew.py -v
```

Attendu : `ModuleNotFoundError: No module named 'skew_analysis'`.

- [ ] **Step 3 : Écrire `01_etl/skew_analysis.py`**

```python
"""
Mesure du decalage entre la source d'entrainement et la source de production.

Le modele s'entrainera sur la reanalyse ERA5 mais predira a partir de l'API
forecast. Ces deux sources ne coincident pas exactement. Une variable dont les
deux sources divergent est un risque : le biais se propagerait dans toutes ses
variables de decalage.

La metrique de decision est MAE / ecart-type : elle rapporte le desaccord a la
variabilite propre de la variable, seule facon de comparer des grandeurs
d'unites differentes.

Usage :
    python 01_etl/skew_analysis.py
"""

from __future__ import annotations

import time as horloge

import pandas as pd
import requests

import config

API_FORECAST = "https://api.open-meteo.com/v1/forecast"
TIMEZONE = "Africa/Tunis"
JOURS_PASSES = 92                     # profondeur maximale de l'API forecast


def recuperer_forecast(lat: float, lon: float, variables: list[str],
                       jours: int = JOURS_PASSES) -> pd.DataFrame:
    """Recupere les heures passees publiees par l'API forecast."""
    reponse = requests.get(
        API_FORECAST,
        params={
            "latitude": lat, "longitude": lon,
            "hourly": ",".join(variables),
            "timezone": TIMEZONE,
            "past_days": jours, "forecast_days": 1,
        },
        timeout=120,
    )
    reponse.raise_for_status()
    df = pd.DataFrame(reponse.json()["hourly"])
    df["time"] = pd.to_datetime(df["time"])
    return df


def calculer_metriques(archive: pd.DataFrame, forecast: pd.DataFrame,
                       variables: list[str]) -> pd.DataFrame:
    """Compare les deux sources sur leurs heures communes.

    Retourne une ligne par variable, triee par desaccord croissant.
    """
    fusion = archive.merge(forecast, on="time", suffixes=("_arc", "_fc"))

    lignes = []
    for variable in variables:
        col_arc, col_fc = f"{variable}_arc", f"{variable}_fc"
        if col_arc not in fusion.columns or col_fc not in fusion.columns:
            continue
        a, f = fusion[col_arc], fusion[col_fc]
        sigma = float(a.std())
        mae = float((f - a).abs().mean())
        lignes.append({
            "variable": variable,
            "n": int(len(fusion)),
            "biais": float((f - a).mean()),
            "mae": mae,
            "sigma": sigma,
            "mae_sur_sigma": mae / sigma if sigma > 0 else float("nan"),
        })

    return (pd.DataFrame(lignes)
            .sort_values("mae_sur_sigma")
            .reset_index(drop=True))


def main() -> int:
    df = pd.read_parquet(config.PARQUET_PROPRE)
    variables = [c for c in df.columns
                 if c not in ("gouvernorat", "time", "latitude",
                              "longitude", "elevation")]

    geo = (df.groupby("gouvernorat", observed=True)[["latitude", "longitude"]]
             .first())

    resultats = []
    for i, (nom, ligne) in enumerate(geo.iterrows(), start=1):
        print(f"[{i:2d}/{len(geo)}] {nom} ...", end=" ", flush=True)
        forecast = recuperer_forecast(ligne["latitude"], ligne["longitude"],
                                      variables)
        archive = df[df["gouvernorat"] == nom][["time"] + variables]
        metriques = calculer_metriques(archive, forecast, variables)
        metriques.insert(0, "gouvernorat", nom)
        resultats.append(metriques)
        print(f"{metriques['n'].iloc[0]:,} heures communes")
        horloge.sleep(2)

    complet = pd.concat(resultats, ignore_index=True)
    complet.to_csv(config.SKEW_CSV, index=False)

    synthese = (complet.groupby("variable", as_index=False)
                .agg(biais=("biais", "mean"), mae=("mae", "mean"),
                     mae_sur_sigma=("mae_sur_sigma", "mean"))
                .sort_values("mae_sur_sigma"))

    print(f"\n--- Synthese sur {len(geo)} gouvernorats ---")
    print(synthese.to_string(index=False,
                             float_format=lambda v: f"{v:8.3f}"))
    print(f"\nDetail par gouvernorat ecrit dans {config.SKEW_CSV.name}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4 : Lancer les tests et vérifier qu'ils passent**

```bash
.venv/Scripts/python.exe -m pytest tests/test_skew.py -v
```

Attendu : 4 tests PASS.

- [ ] **Step 5 : Exécuter sur les 24 gouvernorats**

```bash
.venv/Scripts/python.exe -u 01_etl/skew_analysis.py
```

Attendu : ~2 minutes, une ligne par gouvernorat, puis la synthèse triée.
**Noter le tableau de synthèse** — il servira à fixer le seuil de sélection des
variables et à mettre à jour la documentation.

- [ ] **Step 6 : Stager**

```bash
git add 01_etl/skew_analysis.py tests/test_skew.py data/skew_era5_forecast.csv
```

---

### Task 5 : Notebook d'analyse exploratoire

**Files:**
- Create: `01_etl/notebooks/_build_eda.py`
- Create (généré) : `01_etl/notebooks/01_eda.ipynb`
- Modify: `requirements.txt` (ajout de `nbformat` si absent)

**Interfaces:**
- Consumes: `config.PARQUET_PROPRE`, `config.SKEW_CSV`
- Produces: `01_etl/notebooks/01_eda.ipynb`

Le notebook est **généré** par un script versionné, jamais édité à la main —
même convention que `_build_eda.py` dans `ProjetIntegre`. Les différences Git
restent lisibles et le notebook est reproductible.

**Sections du notebook :**

1. Volumétrie et couverture
2. Qualité des données — les trois anomalies documentées
3. Contrastes régionaux — température et précipitations par gouvernorat
4. Saisonnalité — cycles diurne et annuel
5. **Cohérence entraînement / production** — lit `skew_era5_forecast.csv` et
   produit le tableau qui décide des variables retenues
6. Synthèse et conséquences pour la modélisation

- [ ] **Step 1 : Écrire `01_etl/notebooks/_build_eda.py`**

```python
"""
Genere le notebook d'analyse exploratoire.

Le notebook n'est jamais edite a la main : on modifie ce script et on
regenere. Les differences Git restent ainsi lisibles.

Usage :
    python 01_etl/notebooks/_build_eda.py
"""

from __future__ import annotations

from pathlib import Path

import nbformat as nbf

SORTIE = Path(__file__).resolve().parent / "01_eda.ipynb"


def md(texte: str):
    return nbf.v4.new_markdown_cell(texte.strip())


def code(source: str):
    return nbf.v4.new_code_cell(source.strip())


CELLULES = [
    md("""
# Analyse exploratoire — Tunisia Weather Prediction

Auteur : Ahmed Ben Arfa

Ce notebook est genere par `_build_eda.py`. Ne pas l'editer directement.
"""),

    code("""
import sys
from pathlib import Path

import matplotlib.pyplot as plt
import pandas as pd

RACINE = Path.cwd()
while not (RACINE / "data").exists() and RACINE != RACINE.parent:
    RACINE = RACINE.parent
sys.path.insert(0, str(RACINE / "01_etl"))

import config

pd.set_option("display.width", 140)
plt.rcParams["figure.figsize"] = (11, 4)

df = pd.read_parquet(config.PARQUET_PROPRE)
df["annee"] = df["time"].dt.year
df["mois"] = df["time"].dt.month
df["heure"] = df["time"].dt.hour
print(f"{len(df):,} lignes x {df.shape[1]} colonnes")
"""),

    md("## 1. Volumetrie et couverture"),

    code("""
resume = pd.DataFrame({
    "Caracteristique": [
        "Lignes", "Colonnes", "Gouvernorats", "Debut", "Fin",
        "Heures par gouvernorat", "Valeurs manquantes",
        "Doublons (gouvernorat, heure)",
    ],
    "Valeur": [
        f"{len(df):,}", df.shape[1], df["gouvernorat"].nunique(),
        str(df["time"].min()), str(df["time"].max()),
        f"{df.groupby('gouvernorat', observed=True).size().iloc[0]:,}",
        int(df.isna().sum().sum()),
        int(df.duplicated(["gouvernorat", "time"]).sum()),
    ],
})
resume
"""),

    md("""
## 2. Qualite des donnees

Trois points identifies lors du profilage. Les deux premiers ont une
consequence directe sur la modelisation.
"""),

    code("""
# 2.1 Humidite du sol : verification que la correction a bien ete appliquee
couches = [c for c in df.columns if c.startswith("soil_moisture")]
df[couches].min().to_frame("minimum apres nettoyage")
"""),

    code("""
# 2.2 rain est quasi redondante avec precipitation
ecart = (df["precipitation"] - df["rain"]).abs()
print(f"Lignes ou les deux different : {(ecart > 1e-9).sum():,}")
print(f"Correlation                  : {df['precipitation'].corr(df['rain']):.6f}")
print(f"Lignes avec neige            : {(df['snowfall'] > 0).sum():,}")
"""),

    code("""
# 2.3 Points de grille : certains gouvernorats partagent une latitude
geo = df.groupby("gouvernorat", observed=True)[
    ["latitude", "longitude", "elevation"]].first()
geo[geo["latitude"].duplicated(keep=False)].sort_values("latitude")
"""),

    md("## 3. Contrastes regionaux"),

    code("""
profil = (df.groupby("gouvernorat", observed=True)
          .agg(temp_min=("temperature_2m", "min"),
               temp_moy=("temperature_2m", "mean"),
               temp_max=("temperature_2m", "max"),
               altitude=("elevation", "first"),
               latitude=("latitude", "first"))
          .round(1)
          .sort_values("temp_moy"))
profil
"""),

    code("""
# Precipitations annuelles moyennes, sur les annees completes
completes = df[df["annee"].between(2019, 2025)]
cumul = (completes.groupby(["gouvernorat", "annee"], observed=True)["precipitation"]
         .sum()
         .groupby("gouvernorat", observed=True).mean()
         .sort_values())

ax = cumul.plot(kind="barh", color="#0b3594")
ax.set_xlabel("mm / an")
ax.set_ylabel("")
ax.set_title("Precipitations annuelles moyennes (2019-2025)")
plt.tight_layout()
plt.show()
"""),

    code("""
# L'altitude explique-t-elle mieux la temperature que la latitude ?
print(profil[["temp_moy", "altitude", "latitude"]].corr().round(3))
"""),

    md("## 4. Saisonnalite"),

    code("""
fig, (g, d) = plt.subplots(1, 2, figsize=(13, 4))

(df.groupby("heure", observed=True)["temperature_2m"].mean()
   .plot(ax=g, color="#0b3594"))
g.set_title("Cycle diurne moyen")
g.set_xlabel("heure")
g.set_ylabel("temperature (C)")

(df.groupby("mois", observed=True)["temperature_2m"].mean()
   .plot(ax=d, color="#c44e52"))
d.set_title("Cycle annuel moyen")
d.set_xlabel("mois")
d.set_ylabel("")

plt.tight_layout()
plt.show()
"""),

    code("""
# Amplitude diurne par gouvernorat : le sud saharien se distingue-t-il ?
amplitude = (df.assign(jour=df["time"].dt.date)
             .groupby(["gouvernorat", "jour"], observed=True)["temperature_2m"]
             .agg(lambda s: s.max() - s.min())
             .groupby("gouvernorat", observed=True).mean()
             .round(1).sort_values())
amplitude.to_frame("amplitude diurne moyenne (C)")
"""),

    md("""
## 5. Coherence entrainement / production

Le modele s'entrainera sur la reanalyse ERA5 mais predira a partir de l'API
forecast. **C'est ce tableau qui decide** des variables admises comme features :
une variable dont les deux sources divergent propagerait son biais dans toutes
ses variables de decalage.

La metrique de decision est MAE / ecart-type, seule comparable entre des
grandeurs d'unites differentes.
"""),

    code("""
skew = pd.read_csv(config.SKEW_CSV)
synthese = (skew.groupby("variable", as_index=False)
            .agg(biais=("biais", "mean"), mae=("mae", "mean"),
                 mae_sur_sigma=("mae_sur_sigma", "mean"))
            .sort_values("mae_sur_sigma")
            .round(3))
synthese
"""),

    code("""
ax = synthese.set_index("variable")["mae_sur_sigma"].plot(
    kind="barh", color="#0b3594")
ax.set_xlabel("MAE / ecart-type")
ax.set_ylabel("")
ax.set_title("Desaccord entre archive ERA5 et API forecast")
plt.tight_layout()
plt.show()
"""),

    code("""
# Le desaccord est-il homogene entre gouvernorats ?
dispersion = (skew.groupby("variable")["mae_sur_sigma"]
              .agg(["min", "mean", "max"]).round(3)
              .sort_values("mean"))
dispersion
"""),

    md("""
## 6. Synthese

A completer apres execution :

- variables retenues comme features et seuil retenu ;
- consequences pour le feature engineering de la phase 06 ;
- limites assumees, dont la couverture saisonniere partielle de la comparaison
  inter-sources (l'API forecast ne remonte qu'a 92 jours).
"""),
]


def main() -> None:
    nb = nbf.v4.new_notebook(cells=CELLULES)
    nb.metadata["kernelspec"] = {
        "display_name": "Python 3", "language": "python", "name": "python3",
    }
    nbf.write(nb, SORTIE)
    print(f"Ecrit : {SORTIE}  ({len(CELLULES)} cellules)")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2 : Générer le notebook**

```bash
.venv/Scripts/python.exe 01_etl/notebooks/_build_eda.py
```

Attendu : `Ecrit : ...01_eda.ipynb (19 cellules)`.

- [ ] **Step 3 : Exécuter le notebook de bout en bout**

```bash
uv pip install --python .venv/Scripts/python.exe jupyter nbconvert seaborn
.venv/Scripts/python.exe -m nbconvert --to notebook --execute --inplace 01_etl/notebooks/01_eda.ipynb
```

Attendu : aucune erreur. Si une cellule échoue, corriger `_build_eda.py` puis
régénérer — **ne jamais corriger le `.ipynb` directement**.

- [ ] **Step 4 : Stager**

```bash
git add 01_etl/notebooks/_build_eda.py 01_etl/notebooks/01_eda.ipynb requirements.txt
```

---

### Task 6 : Report des résultats dans la documentation

**Files:**
- Modify: `01_etl/README.md`
- Modify: `README.md`
- Modify: `docs/conception/2026-07-26-conception-projet.md`
- Modify: `06_machine_learning/README.md`

Cette tâche referme la boucle ouverte lors du retrait des chiffres non
reproductibles : maintenant qu'ils sont produits par du code versionné, ils
peuvent revenir dans la documentation.

- [ ] **Step 1 : Reporter le tableau de décalage**

Dans `README.md` et `docs/conception/…` §8.3, remplacer les encarts
« Mesure produite par `01_etl/skew_analysis.py` … » par le tableau de synthèse
issu de l'exécution, en citant sa source :

> Source : `01_etl/skew_analysis.py`, détail par gouvernorat dans
> `data/skew_era5_forecast.csv`, analyse dans `01_etl/notebooks/01_eda.ipynb`.

- [ ] **Step 2 : Fixer et justifier le seuil de sélection**

Choisir le seuil sur `mae_sur_sigma` au vu de la distribution obtenue — chercher
une rupture nette entre variables cohérentes et variables divergentes plutôt
qu'un chiffre rond. Inscrire la liste définitive des variables retenues dans
`06_machine_learning/README.md`, section « Variables retenues ».

- [ ] **Step 3 : Mettre à jour l'état d'avancement**

Dans `README.md`, passer « Contrôles qualité et EDA » de « À venir » à
« Terminé ». Corriger la cartographie CRISP-DM §2 de la conception : le
nettoyage est en phase 01, pas 02.

- [ ] **Step 4 : Régénérer les PDF et stager**

```bash
.venv/Scripts/python.exe 00_documentation/_build_pdf.py
git add -A
```

---

## Auto-revue

**Couverture du spec.** Les quatre livrables annoncés pour la phase ETL sont
couverts : contrôles (tâche 1), nettoyage (tâche 2), décalage inter-sources sur
24 gouvernorats (tâche 4), notebook EDA (tâche 5). L'orchestrateur (tâche 3) et
le report documentaire (tâche 6) referment la chaîne.

**Cohérence des noms.** `config.PARQUET_PROPRE` est produit en tâche 3 et
consommé en tâches 4 et 5. `config.SKEW_CSV` est produit en tâche 4 et consommé
en tâche 5. `checks.verifier_decalage_groupe` est écrit en tâche 1 et resservira
en phase 06 pour valider `build_features`.

**Point de vigilance.** La tâche 4 dépend du réseau et du quota Open-Meteo. En
cas de 429, attendre la réouverture de la fenêtre — voir la gestion dans
`extract_openmeteo.py`. La partie calcul étant isolée dans
`calculer_metriques`, les tests restent exécutables hors ligne.
